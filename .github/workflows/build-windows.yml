name: Build Windows Installer

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version tag (e.g., v1.0.1)'
        required: true
        type: string

permissions:
  contents: write

env:
  APP_NAME: SistemaBPOFinanceiro
  PYTHON_VERSION: '3.12'

jobs:
  build-windows:
    name: Build Windows Installer
    runs-on: windows-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
          pip install pyinstaller

      - name: Build executable with PyInstaller
        run: |
          pyinstaller --clean --noconfirm build.spec

      - name: Verify executable exists
        run: |
          if (Test-Path "dist\${{ env.APP_NAME }}.exe") {
            Write-Host "Executavel encontrado: dist\${{ env.APP_NAME }}.exe"
            $size = (Get-Item "dist\${{ env.APP_NAME }}.exe").Length
            Write-Host "Tamanho: $([math]::Round($size/1MB, 2)) MB"
          } else {
            Write-Error "Executavel NAO encontrado!"
            exit 1
          }

      - name: Install Inno Setup
        run: |
          $url = "https://files.jrsoftware.org/is/6/isetup-6.2.2.exe"
          $installer = "$env:TEMP\isetup.exe"
          Invoke-WebRequest -Uri $url -OutFile $installer
          $args = "/VERYSILENT /SUPPRESSMSGBOXES /NORESTART /SP-"
          Start-Process -FilePath $installer -ArgumentList $args -Wait
          Remove-Item $installer -Force

      - name: Add Inno Setup to PATH
        run: |
          $iscc = "${env:ProgramFiles(x86)}\Inno Setup 6\ISCC.exe"
          if (Test-Path $iscc) {
            echo "$iscc" | Out-File -FilePath $env:GITHUB_PATH -Encoding utf8 -Append
          } else {
            Write-Error "ISCC.exe nao encontrado apos instalacao"
            exit 1
          }

      - name: Determine version
        id: version
        run: |
          if ("${{ github.event_name }}" -eq "workflow_dispatch") {
            $ver = "${{ github.event.inputs.version }}".TrimStart('v')
            echo "version=$ver" >> $env:GITHUB_OUTPUT
            echo "tag_name=v$ver" >> $env:GITHUB_OUTPUT
            echo "release_name=$ver" >> $env:GITHUB_OUTPUT
            echo "is_prerelease=false" >> $env:GITHUB_OUTPUT
          } elseif ("${{ github.ref_type }}" -eq "tag") {
            $ver = "${{ github.ref_name }}".TrimStart('v')
            echo "version=$ver" >> $env:GITHUB_OUTPUT
            echo "tag_name=${{ github.ref_name }}" >> $env:GITHUB_OUTPUT
            echo "release_name=$ver" >> $env:GITHUB_OUTPUT
            echo "is_prerelease=false" >> $env:GITHUB_OUTPUT
          } else {
            # main branch push - use commit short SHA + date
            $date = Get-Date -Format "yyyyMMdd"
            $sha = "${{ github.sha }}".Substring(0, 7)
            $ver = "nightly-$date-$sha"
            echo "version=$ver" >> $env:GITHUB_OUTPUT
            echo "tag_name=nightly-latest" >> $env:GITHUB_OUTPUT
            echo "release_name=Nightly Build $date" >> $env:GITHUB_OUTPUT
            echo "is_prerelease=true" >> $env:GITHUB_OUTPUT
          }

      - name: Build installer with Inno Setup
        run: |
          $version = "${{ steps.version.outputs.version }}"
          iscc /DMyAppVersion=$version installer.iss

      - name: Verify installer
        run: |
          $version = "${{ steps.version.outputs.version }}"
          $installerPath = "dist\installer\${{ env.APP_NAME }}_Setup_$version.exe"
          if (Test-Path $installerPath) {
            $size = (Get-Item $installerPath).Length
            Write-Host "Instalador: $installerPath"
            Write-Host "Tamanho: $([math]::Round($size/1MB, 2)) MB"
            echo "INSTALLER_PATH=$installerPath" >> $env:GITHUB_ENV
          } else {
            Write-Error "Instalador NAO encontrado!"
            Get-ChildItem dist\installer\ -Recurse
            exit 1
          }

      - name: Create checksums
        run: |
          $exePath = "dist\${{ env.APP_NAME }}.exe"
          $instPath = $env:INSTALLER_PATH
          
          $exeHash = Get-FileHash -Algorithm SHA256 $exePath
          $instHash = Get-FileHash -Algorithm SHA256 $instPath
          
          "SHA256 (${{ env.APP_NAME }}.exe) = $($exeHash.Hash)" | Out-File -FilePath "dist\SHA256SUMS.txt" -Encoding utf8
          "SHA256 (${{ env.APP_NAME }}_Setup_${{ steps.version.outputs.version }}.exe) = $($instHash.Hash)" | Out-File -FilePath "dist\SHA256SUMS.txt" -Encoding utf8 -Append
          
          Get-Content "dist\SHA256SUMS.txt"

      - name: Create/Update Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag_name }}
          name: ${{ env.APP_NAME }} ${{ steps.version.outputs.release_name }}
          body: |
            ## ${{ env.APP_NAME }} ${{ steps.version.outputs.release_name }}
            
            **Build:** `${{ steps.version.outputs.version }}`
            **Commit:** `${{ github.sha }}`
            **Branch:** `${{ github.ref_name }}`
            **Data:** ${{ steps.version.outputs.version != 'nightly' && github.event.head_commit.timestamp || '' }}
            
            ### Downloads
            - **Instalador Windows** (recomendado): `${{ env.APP_NAME }}_Setup_${{ steps.version.outputs.version }}.exe`
            - **Executável standalone**: `${{ env.APP_NAME }}.exe`
            
            ### Verificação de integridade
            ```
            $(Get-Content dist\SHA256SUMS.txt)
            ```
            
            ### Instalação
            1. Baixe o instalador
            2. Execute como administrador (necessário para Program Files)
            3. Marque "Criar atalho na área de trabalho" se desejar
            4. O sistema remove automaticamente o aviso do SmartScreen
            
            ### Para desenvolvedores
            - Build reproduzível via GitHub Actions
            - Assinado pelo GitHub (reputação SmartScreen)
            - SHA256 disponível para verificação
          draft: false
          prerelease: ${{ steps.version.outputs.is_prerelease }}
          make_latest: ${{ steps.version.outputs.is_prerelease != 'true' }}
          files: |
            dist/${{ env.APP_NAME }}.exe
            dist/installer/${{ env.APP_NAME }}_Setup_${{ steps.version.outputs.version }}.exe
            dist/SHA256SUMS.txt

      - name: Upload artifacts (manual trigger)
        if: github.event_name == 'workflow_dispatch'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.APP_NAME }}-${{ github.event.inputs.version }}
          path: |
            dist/${{ env.APP_NAME }}.exe
            dist/installer/${{ env.APP_NAME }}_Setup_${{ steps.version.outputs.version }}.exe
            dist/SHA256SUMS.txt
          retention-days: 30